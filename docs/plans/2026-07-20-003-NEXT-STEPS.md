# Next steps — Combat Engine Foundation (after U2 roster-side)

Scratch handoff for the next session. Not a plan artifact; delete once consumed.
Read `docs/AGENTS.md`, `CLAUDE.md`, the plan, **and `2026-07-20-003-amendments.md`**
(A1–A4) first. Tree is green at 918 tests, ruff-enforced.

## State of play

U2 is landed for the **roster member**: engine `Combatant` is blueprint-only
(`name`, `weapon`, `vitality` slot, `attrs`); config `Gangster(Combatant)` holds
the named stats as single-sourced properties; persistence rebuilds a bare
`Combatant`; cross-class `__eq__` + `eq=False` make live/loaded states compare
equal. See amendment A4.

Board: issues #52–#60 (`gh issue list --label plan:combat-foundation`). U1 (#52)
closed. U2 (#53) is **not** done — the Fighter half below remains.

## IMMEDIATE — finish U2: the on-grid `Fighter` (issue #53)

Same strict DoD #1 pattern as the roster `Combatant`, applied to `Fighter`. This
is the last thing between the tree and "no game-stat name in `engine/`".

**The remaining violations (verified against the tree at HEAD `bc31152`):**
- `engine/state/__init__.py:~396` — `Fighter` still has `energie`/`kraft`/
  `brutalitaet` as named dataclass fields (+ `_derive_attrs`/`FIGHTER_ATTR_NAMES`).
- `engine/combat.py:202-204` — `build_player_side` reads `g.energie`/`g.kraft`/
  `g.brutalitaet` off the roster member and names them building the Fighter.
- `engine/combat.py:219-220` — `ENEMY_KRAFT`/`ENEMY_BRUTALITAET = 30` constants
  (plan Finding 4: the engine must name neither the stat nor its value).
- `engine/combat.py:224,240-242` — `build_enemy_side` names `energie`/`kraft`/
  `brutalitaet` constructing enemy Fighters.

**The shape (mirror A4 exactly):**
- `Fighter` keeps blueprint slots (`name`, `weapon`, `position`, `down`,
  `equipment`, `roster_id`, `attrs`) + the **`vitality` slot** (drop `energie`).
  Drop `kraft`/`brutalitaet` as named fields — they live in `attrs`.
- `build_player_side` copies the roster member's `attrs` + `vitality` onto the
  Fighter wholesale (it already receives `Combatant`s), naming no stat.
- `build_enemy_side` must receive the enemy's `attrs`/`vitality` from the caller
  (the scenario/handler), NOT hardcode 30/30. `ENEMY_KRAFT`/`ENEMY_BRUTALITAET`
  are deleted; the fixed 30/30 becomes config/scenario data (Finding 4). The
  handlers already build the enemy spec — they supply the enemy stat defaults.
- `Fighter` likely needs the same `eq=False` + inherited/echoed cross-class
  `__eq__` treatment IF anything compares a live Fighter to a reconstructed one
  (check the SpawnFighter/persistence path — `_NESTED_EFFECT_FIELDS` rebuilds
  `Fighter` at `persistence.py`). Verify before assuming; the roster member
  needed it because of the purity harness.

**Watch (learned the hard way, see the new solution doc
`docs/solutions/architecture-patterns/subclass-across-a-layer-boundary-needs-cross-class-eq.md`):**
- If a live Fighter is ever `==`-compared to a reloaded one and they differ in
  class or double-store a stat, you get the identical-repr-but-unequal cascade.
- `combat.py`'s `shoot` writes vitality via `**{self.vitality_key: vitality}`
  (~`:844` before A4 edits) — confirm it writes the `vitality` slot now, not a
  named field.
- Combat already reads `target.attrs[self.vitality_key]` in most places; the
  gap is construction and the two `build_*_side` helpers.

**Proof-first:** the grep guard is the acceptance test —
`grep -rn 'kraft|brutalitaet|energie|intelligenz|ENEMY_KRAFT' engine/ --include='*.py'`
returns only docstrings citing `mf-prg.bas`. Add the structural guard too
(no bare weapon-id comparison — already clean from U1). Prove each new test red.

**Then close #53** and run simplify + review as for the roster half.

## THEN — the serial chain resumes (U3 → U4 → …)

Dependencies unchanged from the plan/command. After U2 (#53) closes:
- **U3 (#54)** — `CombatResult` carries losses; closes #50. Note: `_finish`
  already returns via the amended path; check what A1's `roster_id`/`CombatResult`
  work left. Read the plan's U3 section fresh.
- **U4 (#55)** — side-relative targeting / `hostile_to`. ⚠ `AI_HUNTS_SIDE = 1` is
  CORRECT per mf-prg.bas:30010 — do not "fix" the pinning test.
- **U5 (#56)** — `Scenario`. A1 already removed `weapon_stats`; the scenario
  carries fully-constructed combatants (their equipment on them). Simpler than
  the plan's version — re-read against amendment A1.
- **U6/#57, U6a/#58, U7/#59, U8/#60** — per the plan, one subagent each, serial.

## Standing rules (unchanged)
- One orchestrator + at most one subagent at a time (user directive; AGENTS.md).
- Never dispatch/commit on a red tree; `make check` is the gate (now ruff-hard).
- Every new test proven by breaking the feature red; restore from a scratch copy,
  never `git checkout`.
- Determinism from inputs, never scripted RNG draws (KTD-6).
- Fidelity questions through the mafia-oracle, quoting the BASIC line.
- Consult the user before any deviation from the plan (including "improvements").
- After each unit: commit → `/ce-simplify-code` → `/ce-code-review` → second commit.
- The plan body is not edited during execution; decisions go in the amendments file.
